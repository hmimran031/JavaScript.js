var onoonPrice = 50;
var vegtablePrice = 30;

// console.log(onoonPrice);
console.log(vegtablePrice);

// Additation************************************
var totalPrice = onoonPrice + vegtablePrice;
console.log(totalPrice);

// Subtraction***********************************
var onoonPrice = 50;
var vegtablePrice = 30;

var totalPrice = onoonPrice - vegtablePrice;
console.log(totalPrice);

// Multiplicaton*********************************
var oragePerPice = 30;
var totalPice = 8;

var totalPrice = oragePerPice * totalPice;
console.log(totalPrice);

// Divition********************************************
var many = 500;
var playre = 9;

var eachPlayer = many / playre;
console.log(eachPlayer);
